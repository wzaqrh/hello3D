向量转换w要等于0,
顶点转换w要等于1

d3d9 对const buffer有大小限制,做skinning时bone matrix表限制为56已经是极限了
D3DCREATE_MIXED_VERTEXPROCESSING是d3d9慢的原因

所有c++本地的cb要注意到hlsl是16字节对齐的

创建Device时假如width,height为0，在nvidia显卡上不会crash，而是画面模糊
画面有阵阵黑块，可能是depth buffer精度不够

屏幕模糊还可能是因为 MVP精度或者数据有问题，导致NDC与屏幕相差0.5个像素，于是发生MIP_LINEAR插值,就模糊了